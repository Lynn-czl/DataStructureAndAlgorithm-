#include<iostream>
#include<vector>
#include<string>
using namespace std;

class Solution {
public:
    vector<int> grayCode(int n) {
        if(n == 0)
            return 0;
        vector<string> vv[2];
        int flag=0;
        vv[0].push_back(t1);
        vv[0].push_back(t2);
    }
};

int main() {
    return 0;
}

